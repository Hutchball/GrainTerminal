# Utils

Reusable utility scripts commissioned by Finn, built by Dave.

Each script does one thing, has a docstring, and is registered in:
`Grain Terminal/_System/AI Team/UTILITY_SCRIPTS.md`

Run scripts from the `Web App/` directory:
```bash
python3 utils/<script_name>.py <args>
```
